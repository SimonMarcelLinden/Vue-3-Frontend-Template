export interface IToken {
    token	: string,
	// iss		: string, // Issuer of the token
	// sub		: string, // Subject of the token
	// iat		: string, // // Time when JWT was issued.
	expiration	: string, // // Expiration time
}
